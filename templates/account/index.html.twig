{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <h1>Mon compte</h1>

    <p>Bienvenue {{ app.user.firstname }} dans votre compte.</p>
    <p>C'est dans cet espace que vous allez pouvoir gérer toutes vos informations personnelles.</p>

    <hr>
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_admin') }}">Espace administrateur</a>
    <br>
    {% endif %}
    <a href="{{ path('app_account_edit_password') }}">Modifier mon mot de passe</a>
    <br>
    <a href="{{ path('app_account_address') }}">Gérer mes adresses</a>
    <br>
    <a href="{{ path('app_account_order') }}">Mes commandes</a>
    <hr>
    
{% endblock %}
