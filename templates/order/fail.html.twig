{% extends 'base.html.twig' %}

{% block title %}Erreur de paiement{% endblock %}

{% block body %}
    <h1>Erreur de paiement</h1>
    <p>Désolé <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong> ...</p>
    <p>
        Il semblerait que votre paiement ait échouée pour votre commande n°<strong>{{ order.reference }}</strong>
        <br>
        Une confirmation d'erreur vient de vous être envoyée par e-mail à l'adresse <strong>{{ order.user.email }}</strong>
        <hr>
    </p>
    <a class="btn btn-success" href="{{ path('app_order')}}">Réessayer le paiement</a>
{% endblock %}
